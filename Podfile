# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NCBookmarkiOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NCBookmarkiOS
    pod 'Alamofire', '~> 4.4'
  target 'NCBookmarkiOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NCBookmarkiOSUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
